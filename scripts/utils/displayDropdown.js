
  /*and tags condition
  if (isOpen == "inactive") {
    listTags(type);
  }
  }
  
  // create CLOSEDROPDOWN 
 closeDropdown() {
    [...open].forEach(elt => elt.querySelector("input").value = "");
    [...open].forEach(elt => elt.style.display = "none");
    [...hidden].forEach(elt => elt.style.display = "flex");
  
  } 
 
}*/